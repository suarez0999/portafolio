---
const { class: className, id } = Astro.props;
---

<section
    id={id}
    data-section={id}
    class={`${className} min-h-screen flex flex-col px-10 md:px-20 py-16 bg-white dark:bg-gray-900 rounded-lg`}
>
    <slot />
</section>
