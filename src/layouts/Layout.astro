<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>Porfolio - Diego Suarez</title>
  </head>
  <body>
    <slot />
  </body>
</html>

<style>
  html,
  * {
    margin: 0;
    padding: 0;
    font-family: "Inter", sans-serif;
    box-sizing: border-box;
  }

  body {
    @apply bg-gray-100 text-gray-800 dark:bg-gray-800 dark:text-gray-200;
    width: 100%;
    height: 100%;
    scrollbar-width: thin; /* Firefox */
    scrollbar-color: #4f46e5 #e5e7eb; /* Firefox */
  }

  /* Scroll personalizado en Chrome, Safari, Edge */
  ::-webkit-scrollbar {
    width: 10px; /* Ancho del scroll vertical */
    height: 10px; /* Ancho del scroll horizontal */
  }

  ::-webkit-scrollbar-track {
    background: #e5e7eb; /* Color del track */
    border-radius: 10px;
  }

  ::-webkit-scrollbar-thumb {
    background-color: #4f46e5; /* Color del thumb */
    border-radius: 10px;
    border: 2px solid #e5e7eb; /* Espacio alrededor del thumb */
  }

  ::-webkit-scrollbar-thumb:hover {
    background-color: #4338ca; /* Color al pasar el cursor */
  }

  /* Para el scroll horizontal */
  ::-webkit-scrollbar-horizontal {
    height: 8px;
  }
</style>

